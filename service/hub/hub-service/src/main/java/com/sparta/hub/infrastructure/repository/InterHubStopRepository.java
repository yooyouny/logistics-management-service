package com.sparta.hub.infrastructure.repository;

import com.sparta.hub.domain.InterHubStop;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InterHubStopRepository extends JpaRepository<InterHubStop, Long> {
}
